// Напишіть шаблонну функцію, що знаходить мінімум двох значень

template<typename T>

        T my_min(T a, T b) {
                 if (a < b) {
                     return a;
                 }
               
                 else {
                      return b;
                 }
        }

int main() {
    return 0;
}
